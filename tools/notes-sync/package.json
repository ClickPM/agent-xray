{
  "name": "notes-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "vault 学习分享/ -> Postgres notes_* 的同步管线(R5)。刻意放在 apps/api 之外:CLAUDE.md 规则 6 不许 Encore app root 下出现无关 .ts 与依赖,且 sharp 这类构建期依赖不该进 api 镜像。",
  "scripts": {
    "sync": "bun --bun src/main.ts"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "pg": "^8.13.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/pg": "^8.11.10",
    "typescript": "^5.8.0"
  }
}
