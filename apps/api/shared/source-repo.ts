// Source 源码 tab 指向的公开仓库(R-SOURCE,所有者裁定 2026-09-08)。
//
// **是代码常量,不进库、不进 MCP 入参**:`GitHub ↗` 拼的是 `<repoUrl>/tree/<sha>` 与 `<repoUrl>/blob/<sha>/<path>`,
// 这个值直接进访客可点的 <a href>,不接受任意字符串。读面(apps/api/source/)把它随快照一起回给前端,
// 前端不写死 —— web 与 api 不手工共享源码(CLAUDE.md 规则 6),类型与值都经生成客户端流动。
export const SOURCE_REPO = "ClickPM/agent-xray";
export const SOURCE_REPO_URL = `https://github.com/${SOURCE_REPO}`;
