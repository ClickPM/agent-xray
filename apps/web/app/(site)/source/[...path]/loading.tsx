// Source 文件页的加载态(画板 2p):骨架对位 2o,右上「正在取 <path>…」由 SourceLoadingNote 从 URL 读。
import { SourceSkeleton } from "@/components/source/SourceSkeleton";

export default function SourceFileLoading() {
  return <SourceSkeleton />;
}
