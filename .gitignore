# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# encore
.encore/
encore.gen/

# bun 跑 vitest 时会在 app root 顺手生成 bun.lock。依赖解析归 npm / package-lock.json
# (CLAUDE.md 规则 11:bun 只是运行时,不接管包管理),提交它等于多一份会漂移的事实来源。
apps/api/bun.lock

# env & secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
.secrets.local.cue
.secrets.*.cue

# build & misc
dist/
*.tsbuildinfo
.DS_Store
Thumbs.db

# logs
*.log
