# 复制为 .env 并 chmod 600;所有真实值不入 Git
POSTGRES_PASSWORD=change-me
AGENT_RO_PASSWORD=change-me
# argon2id 哈希(生成方式见 docs/security.md §4)
ADMIN_PASSWORD_HASH=
# 境内服务器的 LLM 海外中转基址(见 docs/deploy-cn-lightweight.md §3)
LLM_PROXY_BASE=
